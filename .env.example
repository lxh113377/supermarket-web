# 超柿 - 环境变量配置
# 复制此文件为 .env 并填入实际值（仅前端构建用；后端 secret 不走该文件）

# 管理接口/公开发表的后端端点（Cloudflare Pages Functions，部署后固定为以下域名）
# 管理 → /web，公开 → /pub（见 functions/web.js、functions/pub.js）
VITE_CB_API_BASE=https://supermarket-web.pages.dev/web
VITE_CB_PUBLIC_API_BASE=https://supermarket-web.pages.dev/pub

# 管理后台密钥（生产）：
# 在 Cloudflare Dashboard → Pages → supermarket-web → Settings → Variables and Secrets
# 新增 secret 型变量 ADMIN_KEY 配置；或命令：
#   wrangler pages secret put ADMIN_KEY --project-name=supermarket-web
# 本地开发：写入 .dev.vars（已被 .gitignore 忽略，勿提交明文）