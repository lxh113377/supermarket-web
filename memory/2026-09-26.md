# 2026-09-26 — 对标第八轮（同架构对标系 + 承诺/判据接闸）

> 起点 HEAD `d309117`；备份 `_backup/pre-round8-2026-09-26/`（git bundle `--all` + 7 个待改文件原件）。
> 报告：`deliverables/GitHub开源项目对标分析报告-第八轮-2026-09-26.md`

## H5 localStore 读出口拷贝收口（N1，修一类不修一例）

【数据流假设】
来源: `src/localStore.ts:3` 是全站**唯一** import `./data/products-seed` 的文件（实测 `grep -rn "products-seed'" src/` 仅 1 命中）⇒ 种子数据的读出口边界就是 localStore；`getLocalCategories():73-75` 直返 `seedCategories` 模块引用，是 4 个数组读出口中唯一未过 `cloneArray` 的一个（`getLocalProducts:88,94`、`getLocalOrders:148`、`getLocalReviews:188` 均已 clone）。
流向: 消费者 = `src/db/products.ts:39` 与 `:57`（本地模式分类读出口）→ 上层 `useCategories`/管理端下拉/顾客端分类页。改动后仍返回同长度同元素数组，调用方零感知；`tests/` 下当前**无**任何 `getLocalCategories` 用例（实测 grep 0 命中）⇒ 本次要新建断言，否则改动静默。
结构: `Category[]`（`src/types.ts`），浅拷贝容器 `slice()` 即恢复"每次读都是新数组"语义；不深拷贝对象本体 —— 现有调用方只做 `setState` 与 `[...list].sort()`（第七轮实测），风险点是原地 `sort/push/splice` 污染整个会话的种子顺序，浅拷贝已足以切断；深拷贝会让每次读都重建对象，属过度设计。
异常: 若 `seedCategories` 因数据文件改动变成非数组（畸形种子），`cloneArray` 的 `Array.isArray(list) ? slice : []` 已兜底为 `[]`，与既有三个读出口行为一致；TS 类型不匹配则 typecheck 直接红，不会静默。

## H3 浏览器 spec 错误观察器产断言（N3 收口）

【数据流假设】
来源: `tests/e2e/smoke.spec.ts:16-18`（实测）把 `pageerror`/`console error` 只 `console.log` 到 stdout ⇒ 页面抛错 e2e 仍绿；同型出口在 `order-flow.spec.ts`、`product-detail.spec.ts` 各一份；已有正确形态在 `tests/e2e-stub/dashboard-cloud.spec.ts:35`（`page.on('pageerror', e => pageErrors.push(e.message))` + 末尾断言）。
流向: 新 helper `tests/e2e/helpers/watchErrors.ts` ← 被 3 个既有 spec import；`page.on('console'|'pageerror')` 收集 → `expect(errors).toEqual([])` 在 teardown 断言。Playwright 的 expect 失败即该 test 红，从而阻断 `deploy.needs=[..., e2e]`。
结构: 返回 `{ errors: string[], dispose(): void }`；白名单沿用 stub spec 的资源噪声口径（`favicon`/`net::ERR_FAILED` 类），白名单必须锚定具体串，禁泛化词（R236 第三条）。
异常: 若某页当前**已有**存量错误，接入即红 —— 处置是先如实记录并修复，禁止为凑绿放宽白名单（R263 先修判据不改数据，反之判据对时改的是代码）；helper 未 dispose 会跨用例串污染，故每个 spec 用 `test.afterEach` 收口。

## H2 文档一致性判据（新增 CI 闸）

【数据流假设】
来源: 手写数字三处实测 —— `README.md:6` badge `176 passed`、`README.md:17` 与 `docs/ARCHITECTURE.md:16` 的 `/web 管理 30 action`；权威生成物 = `docs/api-contract.json`（`endpoints./web.actions` 实测长度 **31**、`/pub` 8）、用例数 = `tests/*.test.ts(x)` 实数、阈值 = `vite.config.js:56-59`（78/72/74/80）。
流向: 新脚本 `scripts/check-doc-consistency.mjs` 读上述三个真值源 → 正则比对 README/ARCHITECTURE 中的对应数字 → 不一致则 exit 非零；接进 `.github/workflows/ci.yml` 与 `npm run verify` 链。
结构: 判据按"字段×文档位置"列表驱动（action 数 / 用例数 / 文件数 / 覆盖率阈值），每条含期望值的**唯一算法来源**；禁把期望值再抄一遍进脚本（否则脚本自身成第二真相源）。
异常: 生成物缺失（`docs/api-contract.json` 不存在）⇒ 显式 FAIL 并说明缺哪个源（零输入不得记 PASS，R247）；测试文件计数须排除 `tests/e2e*/`（Playwright spec 不计入 vitest 用例数），口径写死在注释里。

## H1 / H4 / M1 / M2 CI 与门禁

【数据流假设】
来源: H1 = `README.md:73` 明文承诺 CI 跑 `verify-backend.mjs`（102 断言）而 `grep -n verify:backend .github/workflows/ci.yml` 实测 **0 命中**；H4 = 对标 microfeed `wrangler deploy --dry-run --strict`，本仓 `grep -rn "dry-run" .github/ scripts/` 实测 0 命中；M1 = `scripts/check-changelog.mjs:10` 的 `MODE` 在 push 事件落 `HEAD~1`；M2 = 第七轮实测的大小写同名覆盖事故。
流向: 四处改动全在"验证链"，不改生产代码路径；CI job `build-and-test` 增加 step ⇒ 影响 `deploy.needs` 的阻断强度（只增不减）。H4 用**占位绑定名**的临时 wrangler 配置，`--dry-run` 不创建/不写入任何真实 Cloudflare 资源。
结构: `ci.yml` 沿用既有 step 形态（name + run + env）与 40 位 SHA 钉版（实测本仓 12 处 `uses:` 已全部钉版，不引入新的未钉版件）；新脚本 exit 码 0=过 / 1=红 / 2=环境不满足（对齐本仓 `ci:status` 的三态约定，禁"取不到数静默 PASS"）。
异常: `verify:backend` 依赖 `node:sqlite`（CI node 22 已在本地容器实测可跑同类）；若 CI 缺该能力则**显式红并说明**，不加 `continue-on-error` 掩盖。M2 大小写自查在 Linux runner 上天生不冲突 ⇒ 判据必须比对 **git 索引**而非文件系统枚举，才能在 ubuntu 上真拦。

## 本轮终态（实测回填，含对本文件上面几处假设的纠偏）

- **H1 前提作废**：`ci.yml:103` 一直是 `node scripts/verify-backend.mjs`，我 grep 的是 npm 别名 `verify:backend` ⇒ "CI 没跑后端契约验证"是自造的假缺口，未做任何改动。根因与教训见全局记忆 `feedback-gate-wiring-grep-by-path.md`。
- **H4 落点更正**：Pages 子命令**没有** `--dry-run`（实测 `wrangler pages deploy --help` 零命中），microfeed 那条 Worker 专属；本仓等价物取 `wrangler pages functions build`（实测产物 88,650B、零鉴权零写资源）。
- **M4 接进 CI 当天即见效**：run `36218839505` 的 `visual` 报 4 failed，而本文件上面记的"16/16 全绿"跑在 09-25 19:50 的过期 `dist-e2e` 上（`reuseExistingServer` 就是那条通路）。删产物重建后本地同 4 红 ⇒ 四条按"重锚白象 46 / 断言改 eager / 色块 skip 不删"处置（`394d680`）。教训见全局记忆 `feedback-prove-artifact-freshness-before-green.md`。
- **终态**：`npm run verify`（新增 `verify:case` / `verify:docs` / `verify:functions` 三段后）exit 0；CI run `36219254275`（`394d680`）与 `36219652058`（`d75a0ac`）均 5 job 全绿含 `deploy`；远端 main `d75a0ac` 与本地同尖；outer 报告 `5c09b19` 已推。
- 本轮另两处自纠：① 台账编辑时拿 `## P0 — 当前阻塞` 整节标题当锚点却没回写 ⇒ 标题静默消失（该形态早已记在 `MEMORY.md` 第 14 条，仍复发一次），已补回并留注；② 拼 `head_sha` 查 run id 时把 SHA 补零成假值 ⇒ 一路 404 白等 4 分钟，改用仓内 `npm run ci:status` 才对。

[skill清单] A-memory-start（入口，实跑）；项目绑定表指定项未触发部署通道（无 wrangler/d1 写操作，`verify:functions` 走的是零资源编译），故未加载 `chaoshi-web-deploy`；router 命中 `consulting-analysis` 经独立判定不采纳（面向市场分析，与代码仓技术对标错配）。
[升级建议] 建议为 `A-project-better` 的"遗留待办盘点"补一条机器判据：台账里每条"已落地"须回指可复现命令与产物新鲜度证据（本轮"本地全绿=旧产物"与"假缺口=grep 别名"两次偏差同源于此）；另有 `chaoshi-web-deploy` §2.1/§5.3 的"私有仓"口径仍待按 public 现状更新（涉及该 skill 的部署判据表述，属需用户过目的规则类改动，未自动改）。

## 收尾（用户选取上述待办后直接执行，四项全部落定）

- `A-project-better` V1.10.0 → **V1.11.0**：① §0.2 加第④条「取证命令的输入若是派生产物，须先证产物比源码新」；② 自检 #1 拆 1a/1b —— 4KB 只钉分卷（本 skill V1.6.0 自述范围），入口壳 `SKILL.md` 单列 8KB。**这条不是放宽阈值**：红项改前 HEAD 即 5580B > 4096B，属扫描面过宽的长期假阳性（入口壳承载 description/triggers，为凑绿裁它＝改数据迁就判据）；两向反例实跑（分卷 6884B → FAIL rc=1；入口壳 8711B → FAIL rc=1 且同轮分卷仍 PASS），改后真身五项全 PASS。
- `chaoshi-web-deploy` 3.9.3 → **3.9.4**：§5.x 四行「私有源仓库」口径按 public 现状更正，**§2.1 不动**（实测该节 09-25 已自行更正，我上轮说它失真又是一次自造假缺口，已记账）；坑表历史条目只加注不改写。
- 两条教训记忆：`feedback-gate-wiring-grep-by-path.md`、`feedback-prove-artifact-freshness-before-green.md` 新建并入索引；`| tail` 吞退出码一条**并入既有** `feedback-exit-code-dies-in-pipes.md`（并行会话当日已入册，避免重复条目），补了它没覆盖的两个面：假 rc 会写进持久台账、报"改后状态"须同给改前基线。
- 原列"挂在用户手上"的第二件（`焚诀/eval/ci_panel_runs.log` noise 判红）**已自行消解**：现况从 `AM` 变 `AD`，文件已不在盘上，`noise=pass`；全程未搬未删他人未提交物。终态四门禁 `mirror=pass noise=pass evolution=pass stub=pass` exit 0，`D:/global_skills` 远端 `9a2f14c`。
- 途中一次现场误判留此防复发：`/tmp/g2.log` 被并行会话覆写（MSYS `/tmp` 是**共享**目录，`C:/tmp` 又不存在），导致我第一次"反例自证"实际没改动任何文件却报 PASS。判据：**bash 与 python 两侧必须同用一条 Windows 绝对路径**，且控制实验要验证"改动确实发生"（本次靠打印改后字节数 6884/8711 才确认）。




## 2026-09-26 第九轮（同指令重发＝整跑，非复核）

【数据流假设】
来源: 外部=2026-09-26 下午 `gh api`（8 仓 repo/workflows/contents 全 200，vercel/commerce contents 404）+ 8 仓 blobless sparse clone 读文件（`raw.githubusercontent.com` 本机 000，未用于结论）；内部=`npm run verify` 与 CI run `36233250786` / `36233342757` 实跑输出。改动对象：新增 `tests/ciWorkflow.test.ts`、`tests/gateFixtures.test.ts`、`scripts/verify-release-parity.mjs`、`.github/workflows/release-parity.yml`、`.github/ISSUE_TEMPLATE/*`，改 `.github/workflows/ci.yml`（搬走放错位的 step）、`vite.config.js`（棘轮 78→79）、`README.md`、`CHANGELOG.md`、`memory/07-next-steps.md`。
流向: 两份新测试进 vitest 收集面（`tests/**` 且非 e2e 目录，故自动被 `npm test` 与 CI 的 `npx vitest run --coverage` 跑到；`coverage.include` 只含 `src/**` ⇒ 不改分母）；`verify:parity` 不进 `verify` 链（它会打线上，本地跑会因 github.io 不可达而假红），只由 `release-parity.yml` 在 CI 完成后调用；`release-parity.yml` 由 `workflow_run: CI completed` 触发 ⇒ 它不阻断任何发布，只做漂移检测（与 `uptime.yml` 同类）。
结构: workflow 文本按**自身文件位置**锚根的脚本必须在夹具仓内复制后再跑；所有行锚定正则先 `replace(/\r\n/g,'/')` 归一（本机工作树全 CRLF、ubuntu runner 是 LF，不归一＝本地红 CI 绿）；parity 基线取发布提交 `%ct`（不是本步起始时刻）；假密钥静态不留完整形态（本仓 pre-commit 与 verify 的 scan-secrets 会扫 `git ls-files`）。
异常: CI 首跑把挂在 deploy job 里的 parity 判据跑红 ⇒ 判为位置错而非代码错，搬独立 workflow 并留"不许搬回"的反向断言；判据自身过严的三处（attributes 紧跟、git 前必须 checkout 正则、runGate 只改 cwd）当场自纠并各留常驻正/反样本；`release-parity.yml` 首稿缺 checkout step ⇒ 已补且由"首步是 Checkout"断言钉住；bundle 不同名分支本机无法造反例 ⇒ 如实记为未覆盖，不假装通过。

> footer 协议两行按 reply_footer.md 落 GM 当日日志（唯一校验载体），此处不重复。



## 2026-09-26 第十轮（同指令第三次整跑；分支→PR 首次）

【数据流假设】
来源: 外部=第九轮同一 blobless sparse clone 通道 + `gh search code "isMailEnable"`（命中 `litemall-core/.../org/linlinjava/litemall/core/notify/NotifyService.java`，纠正子代理给的旧包路径）+ `gh api repos/vitejs/vite/contents/.github/workflows/ci.yml`（curl 直连 raw.githubusercontent 超时 rc=28，未据其下结论）。内部=`npm run verify` 实跑 exit 0、`gh run list`/`gh pr checks` 取 CI 现场。改动对象：新增 `functions/lib/notify.js`、`scripts/check-env-docs.mjs`、`scripts/check-pr-has-tests.mjs`、`docs/env-vars.md` + 四个测试文件；改 `functions/lib/backend.js`（两出口接线）、`functions/web.js`/`pub.js`（传 `waitUntil`）、`scripts/verify-release-parity.mjs`（拆纯函数 + CLI 守卫）、`.github/workflows/ci.yml`（新增 step）、`dispatch.yml`（新增 advisory job）、`package.json`、README/CHANGELOG/ARCHITECTURE/.env.example。
流向: webhook 数据只从"刚落库成功且非幂等命中"的 createOrder 结果流向第三方端点，载荷白名单 6 字段（微信号/备注/付款截图**不出仓**），失败面收敛为结论对象且只进 `console.warn`（不写 D1、不进响应体）；`verify:env` 读 `functions/**`+`src/**` 与 `docs/env-vars.md` 双向对账，进 `verify` 链即进 CI；`pr-advisory` 读 `gh pr list` + `git diff --name-only base..head`，**永远 exit 0**，不改任何发布判据。
结构: 三处新代码一律"导出纯函数 + 夹具"（照 workers-sdk `validate-changesets.ts:18` 形态），使分支可在本机反证；`notify.js` 走 ESM 无 Cloudflare 运行时依赖，故 node/jsdom 可直接 import；行锚定正则先归一 CRLF；`waitUntil` 必须以 `(p)=>context.waitUntil(p)` 绑定后传下去（直传引用在 Worker 里 Illegal invocation）；两个下单出口共用 `maybeNotifyNewOrder` 一处适配器，禁各写一份。
异常: 未配 `ORDER_WEBHOOK_URL` ⇒ `webhookTarget` 返回 null，不发请求、不建 promise、不改状态、不耗重试；非法 URL / `file:` / `data:` 协议视同未配置；投递 5xx、网络抛错、4s 超时全部只出结论；`pr-advisory` 取不到 PR 或 gh 不可用 ⇒ 打印 `SKIPPED` 而非判绿，空集输出"这不是通过，是空集"；`verify:env` 取不到 `docs/env-vars.md` 或代码目录 ⇒ exit 2。本轮两处自纠留账：① `notifyNewOrder` 外层 `.catch` 变异后 16 条全绿 ⇒ 判为够不到的死代码并删除，反证挪到 `deliverNewOrder` 内层（改 throw ⇒ 红 2）；② 插值自查首版正则漏 `- run:` 列表项形态，被自家坏夹具当场暴露 ⇒ 改 `^(-\s+)?run:`。


## 2026-09-26 第十一轮（备份可恢复性 + 数据面事实；含一次推翻自己的自纠 round11_restore_drill）

【数据流假设】
来源: 外部=9 仓取证（子代理开文件给行号，我逐条抽验：`agentic-qe aqe-db-backup.sh:56,58,89-90`、`mattermost import_test.go:58-61`、`Telegram-Drive supporter-backup.yml:58,70,75`、`frankensqlite ci_integrity_check.sh:32-34,41-43`【委】；D1 侧"无人载回断言"【否】）。**本轮最关键的一条是我自己实测的**：同一份 `style-src 'self'` 下三种内联样式机制并列对比 —— markup 属性与 `setAttribute('style')` 被拦（computed 回落 + CSP 违规），`el.style.setProperty` / `el.style.x=` 放行（`7px`/`rgb(30,30,30)`/`0.15s`），而 React 走后者。内部=真生产 dump 载回 `:memory:`（1,449,663B / 949 INSERT / 10 表 / integrity=ok / fk=0）、`db/seed.sql` 解析 54 行、`/pub` 真跑取回 28 条。改动对象：新增 `scripts/verify-backup-restore.mjs`、`scripts/check-csp-static.mjs`、`scripts/check-catalog-facts.mjs` + 三个测试文件；改 `db/schema.sql`（补 `schema_migrations`）、`.github/workflows/d1-backup.yml`（演练前移）、`uptime.yml`（报告型 step）、`ci.yml`（CSP step）、`tests/ciWorkflow.test.ts`（顺序 + 排版 + 插值三组新判据）、`src/{utils/variants.ts,data/variants-demo.ts,utils/spec-options.ts,components/product/VariantPicker.tsx,components/product/RelatedProducts.tsx}`、README/CHANGELOG/ARCHITECTURE/runbook。
流向: 恢复演练读 dump 文本、写一次性内存库（**不落盘、不碰开发库、不发网络**），结论进 stdout 与 `$GITHUB_STEP_SUMMARY`（sha256 + 逐表行数）；`verify:csp` 只读 `index.html`/`public/*.html`，进 `verify` 链即进 CI；`report:catalog` 单向读 `/pub` 与 `db/seed.sql`，**不写任何数据**，漂移只打印；色块轴删除是纯减法（无数据引用 `swatch`，删除后 `tsc` 与 790 用例复验）。
结构: dump 计数必须**语句级**（`splitStatements` 尊重单引号与 `''` 转义、跳过 `--` 行注释），不能用行首匹配；表集对账以 `db/schema.sql` 为真相源、系统表（`sqlite_sequence`/`sqlite_stat1`）另列；目录判据分 `'api'`（已解码数组、不得含下架项）与 `'db'`（JSON 文本、允许 enabled=0）两种形状，混用同时造成假红与漏判；workflow 步骤顺序按"索引先后"断言而非正则判位置（第九轮教训延续）。
异常: dump 文件缺失/空 ⇒ `exit 2`（`frankensqlite` 的 `exit 0` 是写进头注的反面教材）；截断 ⇒ sqlite 抛 `incomplete input` ⇒ 判"不是可恢复备份"（实测行为，不猜"静默丢行"）；0 INSERT 的 schema 尸体 ⇒ 判红；`products` 为空 ⇒ 判红；`/pub` 取不到 ⇒ `exit 2` BLOCKED；seed 解不出行 ⇒ `exit 2`；CSP meta 消失或放宽 ⇒ 判红。两处自造事故已记：① 批量删子串时误用整行删除，连带删掉同行 `id`/`name` ⇒ 一条用例红（只看 tsc 不算过）；② 首版 CSP 判据把"探针被拦"当成"产品被拦"，据此改的 5 个文件全部 `git checkout` 回滚。

补记（同日，PR 侧 CI 抓到的第三处自造事故）：`npm run verify` 本机 `exit 0` 而 ubuntu runner 红在
`Cannot bundle Node.js built-in "node:sqlite"` —— 三份节点级判据测试跑在默认 jsdom 环境，链上带 `node:sqlite`，
本机 Vite 放行、Linux 拒绝。修法＝`tests/backupRestore.test.js` / `tests/catalogFacts.test.js` 顶部
`// @vitest-environment node`。规矩入册：**节点级脚本测试一律显式声明 node 环境，不蹭默认 jsdom**；
并且再次印证第八轮那条——**本机绿不是完成证据，接线进 CI 后必须真跑一次**（本轮是 PR 侧 CI 先红）。

再补一条**必须说清的边界**（防止下一轮把它当已完成）：`d1-backup.yml` 的 visibility 守卫在仓库非
private 时会先响亮失败，走不到导出 ⇒ **恢复演练今天只有本机对真 dump 的证据 + 常驻夹具**，CI 侧真跑
要等备份路线（转回 private 或导出后加密）由用户选定。另：`report:catalog` 已在 runner 上第二次真跑
（run `36240825097`，本机与 runner 两次数值完全一致：28/54、漂移 27+1+22、重复 3+3），第十二轮
"摘掉 continue-on-error"的两轮样本条件已满足，但先确认没有"漂移被误判红"再动。


## 2026-09-26 第十二轮（备份链"绿色零备份"实况 + 存活判据；同指令第四次整跑）· round12_liveness

【数据流假设】
来源: 全部取自 API/CLI 实测，不用推断 —— `gh api repos/…/actions/workflows/d1-backup.yml/runs`（2 次 run 全 success）+ `/actions/runs/{id}/jobs`（`Export remote D1`=skipped、`Upload backup artifact`=skipped）+ `/actions/runs/{id}/artifacts`（`total_count=0`）；盘上唯一真导出 `../_backup/supermarket-d1-pre-specoptions-20260925-184136.sql`（1,449,663B）；`A-project-handoff volume` 体检 5 项（整仓 463,113,937B、07 卷 76,088B）。改动对象：新增 `scripts/check-backup-liveness.mjs` + `tests/backupLiveness.test.js`；改 `.github/workflows/d1-backup.yml`（响亮失败）、`uptime.yml`（存活判据 + `actions: read` + `GH_TOKEN`；catalog 摘掉 continue-on-error）、`tests/ciWorkflow.test.ts`（接线契约组）、`package.json`、README/CHANGELOG/runbook §8.4/07/当日日志。
流向: 存活判据单向读 GitHub Actions API（run 列表 + 每次 run 的 jobs/artifacts，最多补 3 次 jobs 以免 N+1），只输出结论与形态，不写任何数据；`d1-backup` 的响亮失败发生在**导出之前**，所以不会出现"导出了但没报"；catalog 由报告型改为阻断型 ⇒ 同一条 `/pub` 读流，退出码语义升级。
结构: 判据只认 `(步骤 conclusion, artifact 数, run conclusion, 时间新鲜度)` 四元组，且一律用**行首锚定的 YAML 键形态**匹配（注释里的 `actions: read` 不得参与判定）；workflow 标识默认文件名而非数字 ID（免一处会漂移的配置）；豁免必须是显式仓库变量 `BACKUP_SKIP_OK`，缺省为响亮。
异常: 零 run ⇒ 判红；runs 非数组/nowMs 非法/时间戳解不出 ⇒ 各自判红；取不到 API ⇒ exit 2 BLOCKED；artifact 有但导出步骤 skipped ⇒ 不算好；无 steps 数据（list 接口不给）⇒ 不退化成假红；有产物但超 2 天 ⇒ 判"链已停摆"。本次标识 round12_liveness。
补记两处自纠：① 反例"抽掉 actions: read"被**我自己写的 YAML 注释**骗过（注释里就有该短语），判据改成行首锚定键形态；② 变异正则 `/ +actions: read\n/` 在 CRLF 工作文本上匹配不到 ⇒ 抽键成了空操作，夹具先在内存里归一再变异。

第十二轮收尾补记（合并后实况，全部可复算）：PR 24 合并为 `5ecf7fa`；main CI run `36242692574`、dispatch `36242692922`/`36242692592` 与 parity 均已复核；手动触发 `D1 Daily Backup`（run `36242719500`）**如期 failure**，`##[error]` 原文＝"未配置 CF_D1_BACKUP_TOKEN ⇒ 导出与上传都会被跳过，本 run 不会产出任何可核对的备份产物…要么配 token…要么显式设仓库变量 BACKUP_SKIP_OK=true"。官方文档另核实：public 仓 60 天无仓库活动会自动禁用 schedule（`docs.github.com/en/actions/how-tos/manage-workflow-runs/disable-and-enable-workflows`）⇒ 三条 cron 互指不是冗余。反哺落点：`A-project-better` V1.11.0 → **V1.12.0**，核验铁律加第⑤条（产物才算数，不信状态字段；细则新卷 `workflow-s0-verify.part1.md`），全局技能仓提交 `f9d9bb9` 已推送并经镜像复检。
